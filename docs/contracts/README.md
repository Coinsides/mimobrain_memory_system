# docs/contracts/

This folder contains **stable contracts** for the memory system.

Examples:
- MU (.mimo) schema versions
- pointer + locator schema
- snapshot schema
- TaskSpec / TaskResult schema

Principle: contracts should be versioned and backward compatible.
